{
  "name": "universal-hotbox-os",
  "version": "0.1.0",
  "main": "main.js",
  "description": "Universal Hotbox OS MVP - Radial Menu Overlay",
  "scripts": {
    "build": "webpack --mode development",
    "start": "npm run build && powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/kill-hotbox-instances.ps1 && set ELECTRON_DISABLE_GPU=1&& electron ."
  },
  "dependencies": {
    "electron": "^28.0.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "@babel/core": "^7.0.0",
    "@babel/preset-env": "^7.0.0",
    "@babel/preset-react": "^7.0.0",
    "babel-loader": "^9.0.0",
    "webpack-cli": "^7.0.2"
  }
}
